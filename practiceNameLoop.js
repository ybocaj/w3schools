alert("Em Jay");

const MainScreen = () ==> {
	let names = [array of names];
	let isNameFound = false;
	let msg = ' '

while (!isNameFound) {
// keep getting random names
const randomNum = Math.floor(Math.random() * names.length);
msg = `is this ur name? ${names[randomNum])`;
}
